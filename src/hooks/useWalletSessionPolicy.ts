/**
 * Wallet session policy — now a no-op since Privy manages sessions.
 * Kept as a stub to avoid breaking any remaining imports.
 */
export function useWalletSessionPolicy() {
  // Privy handles session lifecycle — nothing needed here.
}
